/// Export format options for data export
enum ExportFormat {
  /// Single CSV file with all data
  csv,

  /// Excel workbook with multiple sheets
  excel,

  /// YAML format for structured data
  yaml,

  /// Mermaid diagram format
  mermaid,

  /// Markdown format for documentation
  markdown,
}
